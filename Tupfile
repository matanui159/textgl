.gitignore
include config.tup

CC_FLAGS += -Iinc

: foreach src/*.c |> !CC |> bin/%B.o {LIB_OBJ}
: {LIB_OBJ} |> !LD -shared |> libtgl$(LIB_EXT) {LIB}

LD_FLAGS += -L. -ltgl

: foreach test/*.c |> !CC |> bin/test/%B.o {TEST_OBJ}
: {TEST_OBJ} | {LIB} |> !LD |> test$(BIN_EXT)